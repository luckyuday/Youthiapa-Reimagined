import React from "react";

const PagenotFound = () => {
  return (
    <div className="w-full min-h-screen text-[5rem] font-[dosis] bg-[var(--seasalt)] text-[var(--redwood)] flex items-center justify-center break-all">
      Page not Found
    </div>
  );
};

export default PagenotFound;
